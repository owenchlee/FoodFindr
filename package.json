{
  "name": "foodfindr",
  "version": "0.1.0",
  "description": "Restaurant recommendation app",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.0",
    "helmet": "^8.3.0"
  }
}
